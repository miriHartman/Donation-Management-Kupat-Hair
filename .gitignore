# התעלמות מתיקיות ספריות בשני הפרויקטים
client/node_modules/
server/node_modules/

# התעלמות מקבצי הגדרות (חשוב מאוד לאבטחה!)
# Environment files - NEVER commit these!
server/.env
server/.env.local
server/.env.production
client/.env
client/.env.local
# client/.env.production CAN be committed (no secrets, only VITE_APP_API_URL)

# קבצי לוג וזבל
npm-debug.log*
.DS_Store
node_modules/ 
